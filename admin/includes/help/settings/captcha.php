<?php if (!defined('IPHORM_VERSION')) exit; ?><h4>Number of characters</h4>
<p>The number of characters of the CAPTCHA image.</p>

<h4>Width (in pixels)</h4>
<p>The width of the CAPTCHA image, in pixels.</p>

<h4>Height (in pixels)</h4>
<p>The height of the CAPTCHA image, in pixels.</p>

<h4>Background color</h4>
<p>The background color of the CAPTCHA image, as a hex color e.g. <code>#FFFFFF</code>.</p>

<h4>Text color</h4>
<p>The text color of the CAPTCHA image, as a hex color e.g. <code>#222222</code>.</p>

<h4>Font</h4>
<p>The CAPTCHA text font.</p>

<h4>Minimum font size</h4>
<p>The font size for each character is random generated between this value and the Maximum font size</p>

<h4>Maximum font size</h4>
<p>The font size for each character is random generated between this value and the Minimum font size</p>

<h4>Minimum letter rotation (degrees)</h4>
<p>Each character is rotated slightly at a random angle between this value and the Maximum letter rotation.</p>

<h4>Maximum letter rotation (degrees)</h4>
<p>Each character is rotated slightly at a random angle between this value and the Minimum letter rotation.</p>